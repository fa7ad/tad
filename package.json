{
  "name": "tad",
  "version": "0.14.0",
  "description": "Lerna monorepo for Tabular data viewer libraries and apps",
  "overrides": {
    "ws": "8.18.1"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.4.2",
    "lerna": "^10.0.1",
    "rimraf": "^5.0.5",
    "run-script-os": "^1.1.6",
    "ts-jest": "^29.2.6",
    "typescript": "^4.8.3"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --ci --hoist --force-local --ignore-prepublish",
    "bootstrap-no-drivers-no-app": "lerna bootstrap --ignore reltab-* --ignore tad --ci --hoist --force-local --ignore-prepublish"
  }
}
